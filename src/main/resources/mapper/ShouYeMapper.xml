<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD com.example.Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.home.demo.mapper.ShouYeMapper">
    <select id="select" resultType="int">
select count(u_id) from t_user
</select>
    <select id="select1" resultType="int">
        select count(c_id) from cleaner
    </select>
    <select id="select2" resultType="int">
        select count(o_id) from orders
    </select>
    <select id="select3" resultType="int">
        select count(p_id) from project
    </select>
</mapper>